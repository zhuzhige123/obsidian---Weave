/**
 * Weave 指南牌组 - 内置教程卡片数据
 * 
 * 本文件包含插件内置教程牌组的所有卡片内容
 * 首次使用时自动创建，帮助用户快速了解插件功能
 * 
 * @version 1.0.0
 */

export interface GuideCardData {
  front: string;
  back: string;
  tags: string[];
}

/**
 * 指南牌组名称
 */
export const GUIDE_DECK_NAME = 'Weave 指南';

/**
 * 指南牌组描述
 */
export const GUIDE_DECK_DESCRIPTION = '插件使用指南和教程，帮助你快速上手 Weave';

/**
 * 指南牌组标签
 */
export const GUIDE_DECK_TAGS = ['教程', '指南'];

/**
 * 指南卡片数据
 */
export const GUIDE_CARDS: GuideCardData[] = [
  // 1. 题型识别规则
  {
    front: '插件如何根据卡片内容自动识别题型？',
    back: `**题型判定规则**（按优先级）：

1. **挖空题 (Cloze)**：内容含 \`{{c1::...}}\` 或 \`==文本==\`
2. **选择题 (Choice)**：内容含 \`---choice---\` 或选项行 \`A)\` / \`B)\` 等
3. **基础问答 (Basic)**：以上都不满足时的默认类型

> 判定发生在创建卡片时，由 \`detectCardTypeFromContent()\` 执行`,
    tags: ['题型', '格式']
  },
  
  // 2. 问答题格式
  {
    front: '如何编写标准的问答题卡片？',
    back: `**标准格式**：

\`\`\`
问题内容（正面）

---div---

答案内容（背面）
\`\`\`

**说明**：
- \`---div---\` 是正反面分隔符
- 如果没有分隔符，整段内容作为正面，背面为空`,
    tags: ['题型', '问答题']
  },
  
  // 3. 挖空题格式
  {
    front: '插件支持哪两种挖空标记？',
    back: `**两种挖空标记**：

1. **Obsidian 高亮挖空**：\`==挖空内容==\`
2. **Anki 标准挖空**：\`{{c1::挖空内容}}\` 或 \`{{c1::挖空内容::提示}}\`

**渐进式挖空约束**：
- 使用 \`{{cN::...}}\` 时，编号需从 \`c1\` 开始连续递增
- 例如：\`{{c1::第一个}}\` → \`{{c2::第二个}}\` → \`{{c3::第三个}}\``,
    tags: ['题型', '挖空题']
  },
  
  // 4. 选择题格式
  {
    front: '如何编写标准的选择题卡片？',
    back: `**标准格式**：

\`\`\`
Q: 问题内容
A) 选项1
B) 选项2 {✓}
C) 选项3
D) 选项4

---div---

解析内容（可选）
\`\`\`

**正确答案标记**：
- 标准：\`{✓}\`
- 兼容：\`{✔}\` / \`{correct}\` / \`✓\` / \`✔\``,
    tags: ['题型', '选择题']
  },
  
  // 5. 主要分隔符
  {
    front: '插件使用的核心分隔符有哪些？',
    back: `| 分隔符 | 用途 |
|--------|------|
| \`---div---\` | 正反面分隔（主分隔符） |
| \`---meta---\` | 元数据分区（可选） |

**语义标记前缀**（可选）：
- \`💡 Hint:\` / \`💡 Context:\`
- \`Explanation:\`
- \`Tags:\` / \`Source:\` / \`Difficulty:\``,
    tags: ['格式', '分隔符']
  },
  
  // 6. 如何创建牌组
  {
    front: '在插件中如何创建新牌组？',
    back: `**创建方式**：
1. 打开"创建牌组"弹窗
2. 填写牌组名称（必填）
3. 可选填写牌组标签（用于分类）

**技术说明**：
- 当前只支持创建"根牌组"（无父子层级）
- 数据保存到 \`weave/memory/decks.json\``,
    tags: ['牌组', '创建']
  },
  
  // 7. 选中文本创建卡片
  {
    front: '如何从选中的文本快速创建卡片？',
    back: `**使用命令**：\`Create Card from Selection\`

**执行流程**：
1. 获取选中文本（编辑模式优先，降级到阅读模式）
2. 获取当前文档路径作为来源
3. 在源文档中创建块链接 \`^blockId\`
4. 生成卡片内容（选中文本作为正面）
5. 打开创建卡片模态窗

**降级策略**：
- 无选中文本 → 使用当前行内容
- 无法创建块链接 → 降级为文档级来源`,
    tags: ['卡片', '快捷键']
  },
  
  // 8. 来源链接格式
  {
    front: '卡片的来源信息如何存储？',
    back: `来源信息存储在卡片 YAML frontmatter 的 \`we_source\` 字段：

**两种格式**：
1. **块级来源**：\`we_source: ![[文档名#^blockId]]\`
2. **文档级来源**：\`we_source: [[文档名]]\`

> 块级来源可以精确跳转到源文档的特定位置`,
    tags: ['来源', '链接']
  },
  
  // 9. 数据存储位置
  {
    front: '插件的数据文件存储在哪里？',
    back: `**两个存储位置**：

| 位置 | 内容 | 是否同步 |
|------|------|----------|
| \`weave/\` | 牌组、卡片、学习数据 | ✅ 会同步 |
| \`.obsidian/plugins/weave/\` | 配置、缓存、索引 | ❌ 不同步 |

**常用路径**：
- 牌组：\`weave/memory/decks.json\`
- 卡片：\`weave/memory/cards/\`
- 设置：\`.obsidian/plugins/weave/config/settings.json\``,
    tags: ['数据', '存储']
  },
  
  // 10. 记忆算法
  {
    front: '插件使用什么记忆算法？',
    back: `**FSRS6（Free Spaced Repetition Scheduler 6.1.1）**

**核心参数**：
- \`requestRetention\`：目标记忆保留率（默认 0.9）
- \`maximumInterval\`：最大复习间隔（默认 365 天）
- \`enableFuzz\`：是否启用模糊（避免卡片堆积）
- \`w\`：21 个权重参数（控制算法行为）`,
    tags: ['算法', 'FSRS']
  },
  
  // 11. FSRS 卡片状态字段
  {
    front: 'FSRS 算法在卡片上保存哪些状态？',
    back: `**card.fsrs 字段**：

| 字段 | 含义 |
|------|------|
| \`due\` | 下次复习日期 |
| \`stability\` | 记忆稳定性 |
| \`difficulty\` | 卡片难度 |
| \`elapsedDays\` | 距上次复习天数 |
| \`scheduledDays\` | 计划间隔天数 |
| \`reps\` | 复习次数 |
| \`lapses\` | 遗忘次数 |
| \`state\` | 卡片状态（新/学习/复习/重学） |
| \`lastReview\` | 上次复习时间 |
| \`retrievability\` | 当前可提取性 |`,
    tags: ['算法', 'FSRS']
  },
  
  // 12. YAML 元数据字段
  {
    front: '卡片 YAML frontmatter 支持哪些元数据字段？',
    back: `**支持的字段**：

| 字段 | 说明 |
|------|------|
| \`we_source\` | 来源链接（文档或块） |
| \`we_decks\` | 所属牌组列表 |
| \`we_type\` | 卡片类型 |
| \`we_priority\` | 优先级 |
| \`we_difficulty\` | 难度 |
| \`we_created\` | 创建时间 |
| \`tags\` | 标签列表 |`,
    tags: ['元数据', 'YAML']
  },
  
  // 13. Content-Only 架构
  {
    front: '什么是 Content-Only 架构？',
    back: `**核心理念**：\`content\` 是卡片的**唯一数据源**

**特点**：
- 元数据嵌入 YAML frontmatter（不再使用 \`fields\` 字段）
- 题型由内容中的符号动态解析
- 卡片可以属于多个牌组（通过 \`we_decks\`）

**好处**：
- 数据结构简单
- 易于导出/导入
- 支持 Markdown 原生编辑`,
    tags: ['架构', 'Content-Only']
  },
  
  // 14. 牌组与卡片的关系
  {
    front: '牌组和卡片之间是什么关系？',
    back: `**双向引用架构**：

1. **牌组 → 卡片**：牌组存储 \`cardUUIDs\` 列表（正向引用）
2. **卡片 → 牌组**：卡片 YAML 存储 \`we_decks\` 列表（反向引用）

**特点**：
- 一张卡片可以属于多个牌组
- 卡片独立于牌组存在（删除牌组不会删除卡片）
- 通过 \`ReferenceDeckService\` 管理引用同步`,
    tags: ['架构', '牌组']
  },
  
  // 15. 块链接机制
  {
    front: '块链接 (Block Link) 是如何工作的？',
    back: `**创建流程**：
1. 选中文本后触发创建卡片命令
2. \`BlockLinkManager\` 在源文档中插入 \`^blockId\`
3. 卡片保存块链接引用 \`![[文档#^blockId]]\`

**块 ID 格式**：
- 自动生成的唯一标识符
- 添加在源文档段落末尾
- 例如：\`这是一段文字 ^abc123\`

**用途**：
- 点击卡片来源可跳转到原文精确位置
- 支持双向链接和溯源`,
    tags: ['链接', '块链接']
  },
  
  // 16. 插件定价
  {
    front: 'Weave 插件的定价方案是什么？',
    back: `**定价方案**：

| 版本 | 价格 |
|------|------|
| 早期支持者价格 | ¥46 |
| 标准价格 | ¥64 |
| 完全体最终价格 | ¥78 |

> 早期支持者可享受优惠价格，后续功能更新免费`,
    tags: ['定价', '购买']
  },
  
  // 17. 批量解析功能
  {
    front: '什么是批量解析？支持哪些模式？',
    back: `**批量解析**：从 Markdown 文件中自动提取卡片内容

**三种解析模式**：

1. **单文件单卡片模式**
   - 一个 Markdown 文件对应一张卡片
   - UUID 存储在 frontmatter 中
   - 支持 front-back 分离

2. **单文件多卡片正则模式**
   - 使用自定义正则表达式解析
   - 支持分隔符模式和完整模式
   - 支持标签判断和完全同步

3. **兼容模式（旧解析器）**
   - 保持向后兼容性
   - 适用于未配置新模式的旧映射`,
    tags: ['批量解析', '导入']
  },
  
  // 18. 文件夹牌组映射
  {
    front: '如何配置批量解析的文件夹映射？',
    back: `**映射配置项**：

| 字段 | 说明 |
|------|------|
| \`path\` | 文件夹或文件路径 |
| \`targetDeckId\` | 目标牌组 ID |
| \`includeSubfolders\` | 是否递归子文件夹 |
| \`enabled\` | 是否启用 |
| \`autoCreateDeck\` | 牌组不存在时自动创建 |

**使用流程**：
1. 在设置中添加文件夹→牌组映射
2. 执行"批量解析"命令
3. 插件自动扫描并创建/更新卡片`,
    tags: ['批量解析', '映射']
  },
  
  // 19. 学习队列生成
  {
    front: '学习队列是如何生成的？',
    back: `**队列生成逻辑**：

1. **过滤父卡片**：渐进式挖空的父卡片不参与学习
2. **到期判断**：默认只包含到期的卡片
3. **Bury Siblings**：同一父卡片的子卡片分散复习
4. **标签过滤**：支持包含/排除特定标签

**队列类型**：
- 普通卡片（Basic、Cloze、Choice）
- 渐进式挖空子卡片（ProgressiveChild）`,
    tags: ['学习', '队列']
  },
  
  // 20. 兄弟卡片分散
  {
    front: '什么是兄弟卡片分散（Sibling Dispersion）？',
    back: `**作用**：避免同一知识点的多张挖空卡片在同一天复习，减少记忆干扰

**科学依据**：基于 Anki 的"Disperse Siblings"机制

**默认配置**：
- 最小间隔：5 天
- 动态分散：间隔的 5%（取较大值）
- 在 FSRS 的 fuzz 范围内调整

**两种模式**：
- **P0 队列过滤**：生成队列时自动过滤兄弟卡片
- **P2 复习后调整**：复习完成后动态调整兄弟卡片的 due 日期`,
    tags: ['学习', '兄弟卡片']
  },
  
  // 21. 渐进式挖空
  {
    front: '什么是渐进式挖空（Progressive Cloze）？',
    back: `**定义**：一张卡片包含多个挖空，每个挖空作为独立子卡片学习

**V2 架构**：
- **父卡片（Parent）**：存储完整内容，不参与学习
- **子卡片（Child）**：每个挖空对应一张，独立调度

**自动转换**：
- 检测到多个挖空（\`{{c1::}}\` \`{{c2::}}\`）时自动转换
- 通过 \`ProgressiveClozeGateway\` 统一处理

**使用场景**：
- 新建卡片模态窗
- 批量解析扫描
- APKG/AnkiConnect 导入
- AI 制卡生成`,
    tags: ['挖空题', '渐进式']
  },
  
  // 22. 渐进式挖空格式
  {
    front: '如何编写渐进式挖空卡片？',
    back: `**格式示例**：

\`\`\`
人体最大的器官是{{c1::皮肤}}，最小的骨头是{{c2::镫骨}}。
\`\`\`

**自动生成**：
- 子卡片 1：学习"皮肤"
- 子卡片 2：学习"镫骨"

**编号规则**：
- 从 \`c1\` 开始连续递增
- 每个编号生成一张独立子卡片
- 子卡片继承父卡片的牌组和标签`,
    tags: ['挖空题', '渐进式']
  },
  
  // 23. 图片遮罩编辑
  {
    front: '如何使用图片遮罩功能？',
    back: `**功能**：在图片上添加遮罩区域，创建图片挖空题

**编辑器功能**：
- 添加矩形/圆形遮罩
- 调整遮罩位置和大小
- 设置遮罩样式（颜色、透明度）

**学习时行为**：
- 问题模式：显示所有遮罩
- 答案模式：揭示遮罩内容（支持动画）
- 交互模式：点击单个遮罩切换显示

**数据存储**：
- 遮罩数据以注释形式存储在 Markdown 中
- 与图片关联，支持多图片多遮罩`,
    tags: ['图片', '遮罩']
  },
  
  // 24. 图片遮罩格式
  {
    front: '图片遮罩数据如何存储？',
    back: `**存储位置**：Markdown 内容中的 HTML 注释

**遮罩属性**：
- \`id\`：遮罩唯一标识
- \`type\`：形状类型（rect/circle）
- \`x\`, \`y\`：位置坐标
- \`width\`, \`height\`：尺寸
- \`fill\`：填充颜色
- \`style\`：样式属性

**渲染流程**：
1. 解析 Markdown 内容
2. 查找遮罩注释数据
3. 为对应图片渲染遮罩层`,
    tags: ['图片', '遮罩']
  }
];
